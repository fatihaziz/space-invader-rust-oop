{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "cargo fmt && run",
            "type": "shell",
            "command": "cargo fmt && cargo run",
            "group": {
                "kind": "build",
                "isDefault": true
            },
        }
    ]
}